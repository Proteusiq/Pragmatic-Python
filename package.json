{
  "dependencies": {},
  "devDependencies": {
    "prettier": "3.5.3"
  },
  "scripts": {
    "build": "echo 'GitHub Pages will build the site automatically when you push to the repository'",
    "jekyll:serve": "JEKYLL_ENV=development bundle exec jekyll serve",
    "jekyll:build": "JEKYLL_ENV=production bundle exec jekyll build"
  }
}
